# Python
__pycache__/
*.pyc
.venv/
dist/

# Data — tracked by DVC, not git
data/raw/
data/processed/

# Artifacts — keep models, figures and data the Streamlit app needs at runtime
artifacts/*
!artifacts/model.onnx
!artifacts/model.onnx.data
!artifacts/ppi_model.onnx
!artifacts/recurrent_model.onnx
!artifacts/comparison_snr.png
!artifacts/paper_roc.png
!artifacts/comparison_results.json
!artifacts/comparison_runtime.json
!artifacts/calibration.json
!artifacts/ppi_metrics.json
!artifacts/recurrent_metrics.json
!artifacts/roc_comparison_1d.json

# Experiment tracking — mlruns tracked so Streamlit Cloud has run history
mlflow.db

# Secrets
.env
.streamlit/secrets.toml

# Local archives / scratch
*.zip

# DVC internals (keep .dvc/ itself, ignore cache)
.dvc/cache/
.dvc/tmp/artifacts/
*.aux

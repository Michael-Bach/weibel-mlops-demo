# Python
__pycache__/
*.pyc
.venv/
dist/

# Data — tracked by DVC, not git
data/raw/
data/processed/

# Artifacts — ignore everything except the deployed ONNX model for Streamlit
artifacts/*
!artifacts/model.onnx
!artifacts/model.onnx.data

# Experiment tracking
mlruns/

# Secrets
.env

# DVC internals (keep .dvc/ itself, ignore cache)
.dvc/cache/
.dvc/tmp/artifacts/

[tool.ruff]
line-length = 100
target-version = "py311"
select = ["E", "F", "I", "UP"]
exclude = [".git", "__pycache__", ".dvc"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
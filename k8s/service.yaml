apiVersion: v1
kind: Service
metadata:
  name: radar-inference
spec:
  selector:
    app: radar-inference
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP  # use LoadBalancer to expose externally
